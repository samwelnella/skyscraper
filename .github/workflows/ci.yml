name: Tests

on: [push]

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@master
      - name: Install deps
        run: ./scripts/deps
      - name: Build
        run: ./update_skyscraper.sh
      - name: Test
        run: ./scripts/test
